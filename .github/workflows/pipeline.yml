name: Use Python CI/CD Pipeline

on:
  push:
    branches:
      - feature/demo-testing

jobs:
  run_pipeline:
    uses: aeiforo-co-uk/dt-module-python-pipeline-repo/.github/workflows/python_pipeline.yml@feature/DD-20
    with:
      repo_name: "dt-module-python-pipeline-repo"
      deploy_env: "DEV"
      build_artifacts: true
      python_version: "3.9"
